<div class="main">
  <div class="inner">
    <div class="form__wrapper">
      <h2 class="page-heading">ユーザー新規登録</h2>

        <%# 「モデル名」にはUserモデルであれば@userを渡しましょう。%>
        <%# 「新規登録機能へのパス」は、devise導入後にrails routesを実行してdevise/registrations#createへのパスを確認し、記載してください。 %>
        <%= form_with model: @user, url: user_registration_path, local: true do |f| %>
      <%= devise_error_messages! %>

        <div class="field">
          <%= f.label :email, "メールアドレス" %><br />
          <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
        </div>

        <div class="field">
          <%= f.label :password, "パスワード（6文字以上）" %><br />
          <%= f.password_field :password, autocomplete: "new-password" %>
        </div>

        <div class="field">
          <%= f.label :encrypted_password, "パスワード再入力" %><br />
          <%= f.password_field :encrypted_password, autocomplete: "new-password" %>
        </div>

        <div class="field">
          <%= f.label :name, "ニックネーム" %><br />
          <%= f.text_field :name %>
        </div>

        <div class="field">
          <%= f.radio_button :skill, :beginner %>
          <%= f.label :skill, "ビギナー", {value: :beginner , style: "display: inline-block;"} %>
          <%= f.radio_button :skill, :intermediate %>
          <%= f.label :skill, "中級者", {value: :intermediate, style: "display: inline-block;"} %>
          <%= f.radio_button :skill, :master %>
          <%= f.label :skill, "名人", {value: :master, style: "display: inline-block;"} %>
        </div>


        <div class="actions">
          <%= f.submit "新規登録", class: :form__btn  %>
        </div>
      <% end %>
    </div>
  </div>
</div>