<div class="main">
  <div class="user_page">
    <div class="user__wrapper_header">
      <%= link_to "トップページに戻る", root_path, class: "nav__btn" %>
      <h2 class="page-heading">
        <%= "#{@user.nickname}さんの情報"%>
      </h2>
      <div class="user_details">
        <% if @user.posts.present? %>
        <%= "累計釣果記録#{@fishing_result_sum}匹" %>
        <br><%= "一番釣れた日は#{@max_fishing_result_details.created_at.strftime('%Y/%m/%d')}
                        (#{@max_fishing_result_details.weather.name})"%><br>
        <%= "釣果は#{@max_fishing_result}匹"%><br>
        <%= "その時のエサは#{@max_fishing_result_details.feed.name}で、"%>
        <%= "釣り場は#{@max_fishing_result_details.place.name}で、棚は水面から#{@max_fishing_result_details.water_depth.name}付近でした。"%>                   
      </div>
    </div>
    <div class="user__wrapper">
        <h2 class="page-heading">
          <%= "#{@user.nickname}さんの釣果情報"%>
        </h2>
        <div class="users-box">
          <ul class='user-lists'>
            <% @posts.each do |post|%>
              <li class='user-list'>
                <%= link_to post_path(post.id) do %>
                    <%= image_tag post.image, class: "user-post-img" if post.image.attached?%>
                    <% unless post.image.present? %>
                    <div class='user-post-img'>
                      <%= image_tag "image7.png", class: "item-img" %>
                    </div>
                    <% end %>
                <% end %>
                <span>
                  <%= "釣果：#{post.fishing_result}匹" %>
                </span>
              </li>
            <% end %>
          </ul>
        </div>
      <% else %>
          <div class="no_information">
          まだ投稿されたことがありません！
          <br>いっぱい釣ってワカサギ釣りを楽しみましょう！！！
          <br>釣れた情報を共有してワカサギ釣りを楽しみましょう！！！
          </div>
      <% end %>
    </div>
  </div>
</div>